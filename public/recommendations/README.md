Generated recommendation thumbnails live here.

- `images/` stores the downloaded files.
- `manifest.json` maps a deterministic recommendation key to a file name.

Use:

```bash
npm run fetch:recommendation-images
```
